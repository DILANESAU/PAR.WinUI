<Window
    x:Class="PAR.WinUI.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="PAR Intelligence - Login">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#0F172A" Offset="0.0"/>
                    <GradientStop Color="#1E293B" Offset="1.0"/>
                </LinearGradientBrush>
            </Grid.Background>

            <Ellipse Width="300" Height="300" Fill="White" Opacity="0.05" Margin="-100,-100,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <Ellipse Width="200" Height="200" Fill="White" Opacity="0.05" Margin="0,0,-50,-50" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="5">
                <Image Source="/Assets/logoPAR.png" Width="176" Height="110" HorizontalAlignment="Center"/>

                <TextBlock Text="PAR INTELLIGENCE" Foreground="White" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="Sistema de Análisis Comercial" Foreground="White" FontSize="14" Opacity="0.8" HorizontalAlignment="Center"/>

                <Border Background="White" Height="1" Width="50" Opacity="0.5" Margin="0,20"/>

                <TextBlock Text="v1.0" Foreground="White" FontSize="10" Opacity="0.5" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Column="1" Padding="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="1" VerticalAlignment="Center" Spacing="20">
                <StackPanel Spacing="5" Margin="0,0,0,10">
                    <TextBlock Text="Bienvenido" FontSize="24" FontWeight="Bold"/>
                    <TextBlock Text="Ingresa tus credenciales para continuar" FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>

                <TextBox Header="Usuario" 
                         PlaceholderText="Ingresa tu usuario"
                         Text="{x:Bind ViewModel.Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="14"/>

                <PasswordBox x:Name="TxtPassword"
                             Header="Contraseña" 
                             PlaceholderText="Ingresa tu contraseña"
                             FontSize="14"/>

                <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" 
                           Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                           FontSize="12" TextWrapping="Wrap"/>

                <Button Click="BtnLogin_Click"
                        HorizontalAlignment="Stretch"
                        Height="50"
                        CornerRadius="8"
                        Background="#6D28D9"
                        BorderBrush="#7C3AED"
                        Foreground="White">

                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <ProgressRing IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" 
                                      Foreground="White" 
                                      Width="20" Height="20"
                                      Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay}"/>

                        <TextBlock Text="INICIAR SESIÓN" VerticalAlignment="Center" FontWeight="Bold"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <TextBlock Grid.Row="2" Text="© 2026 PAR Pinturas" HorizontalAlignment="Center" FontSize="10" Foreground="{ThemeResource TextFillColorDisabledBrush}"/>
        </Grid>
    </Grid>
</Window>